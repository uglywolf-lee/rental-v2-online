# 대림빌딩 부동산 관리 시스템 — 프로젝트 안내

이 파일은 새 세션이 이 폴더를 열 때 자동으로 읽힙니다.
과거 대화를 다시 읽을 필요 없이, 여기 내용만으로 작업을 이어갈 수 있어야 합니다.

---

## 0. 가장 중요한 원칙 (어기지 말 것)

1. **"누구나·어르신도 쉽게"** — 사용자는 컴퓨터에 익숙하지 않은 임대인·직원입니다.
   전문용어 금지, 화면에 기술 노출 금지, 삭제/초기화 버튼 금지.
2. **데이터는 절대 잃어버리면 안 됩니다.** 자동 백업 유지, DB 덮어쓰기 금지.
3. **사람은 오타를 냅니다.** 저장한 값은 반드시 입력창에 되불러와 그 자리에서 고칠 수 있어야 합니다.
4. **실행 스크립트는 파일명·내용 모두 영문(ASCII)으로 만드세요.**
   `.bat` `.command` `.sh` 전부 해당합니다. (윈도우 인코딩 오류로 두 번 깨진 전례,
   cron이 한글 경로를 못 찾는 위험) 사용자가 읽는 자료 파일(`백업기록.txt` 등)은 한글로 둡니다.
5. **파이썬 실행을 사용자에게 시키지 마세요.** 더블클릭 한 번으로 끝나야 합니다.
6. 수정 후에는 항상 **JS 문법 검사 → 서버 기동 → HTTP 200 확인 → claude_부동산 폴더 동기화** 순으로 검증합니다.
7. **답변은 항상 한글로 합니다.** 설명·요약·질문 모두 한국어로 쓰세요. (코드·명령어·파일명은 원문 그대로)

---

## 1. 무엇을 만드는가

- **대상 건물**: 대림빌딩 (서울특별시 영등포구 도림로 140), 74개 호실
- **사용자**: 임대인 1명(이은석, 1972년생) + 현장 직원 소수
- **형태**: 인터넷 없이 도는 로컬 프로그램. 윈도우 PC에서 EXE 더블클릭 → 브라우저로 접속
- **기술**: 파이썬 표준 라이브러리 HTTP 서버 + SQLite. 외부 프레임워크 없음
- **포트**: **8899**

## 2. 파일 구조

| 파일 | 역할 |
|---|---|
| `server.py` | 시작점. 포트 8899, 정적파일 서빙, `/api/v1/serverinfo` |
| `db.py` | 스키마 · 마이그레이션 · 자동백업 |
| `routes.py` | REST API `/api/v1/...`, 감사로그, 스냅샷 |
| `index.html` | 로그인 관문 (모든 접속이 여기를 거침) |
| `guard.js` | 로그인 검사 + `_actor` 자동 주입 + **서류 창 열기(`openDocs`)** |
| `doc_viewer.html` | 📄 계약서·신분증 별도 창 (모니터 옆에 두고 보는 용도) |
| `main.html` | 메뉴 |
| `interface-a.html` | 호실 현황 |
| `contract_master.html` | 계약서 등록·수정 |
| `contractor_roster.html` | 계약자 명부 + **명세서 출력** |
| `utility_bills.html` | 공과금 검침 (탭 3개를 iframe으로 품고 있음) |
| `kepco_meter.html` | ⚡ 한전 검침표 (74행, 엑셀·메일) |
| `water_bill.html` | 💧 수도요금 배분 |
| `monthly_rent_collection.html` | 월세 수납 |
| `g_h_i_dashboard.html` | 통합 대시보드 |
| `daily_report.html` | 금일 현황 (인쇄용) |
| `team_management.html` | 직원·협력사 |
| `incidents_maintenance.html` | 유지보수 |
| `build_windows.bat` | 윈도우 EXE 빌드 (**ASCII 전용**) |
| `UPDATE_apply.bat` | 현장 업데이트 (DB는 건드리지 않음) |
| `update-pi.command` | **맥 → 오렌지파이 업데이트.** 더블클릭 한 번. 홈 폴더에도 사본 |
| `오렌지파이_명령어.txt` | 접속·재시작·로그·백업점검 명령 모음 (사용자용) |
| `필독_사용안내.txt` | 비전문가용 설명서 |

## 3. 꼭 알아야 할 업무 규칙

- **부가세 = 관리비 × 10%.** 월세와 공과금 실비는 제외합니다.
  **월세에 부가세를 붙이지 마세요.** 계약할 때 세입자가 깎아 달라고 해서
  **월세는 부가세 포함 금액으로 합의**하고 받습니다. 여기에 10%를 또 붙이면 이중청구입니다.
  계약서의 `documents_json.vat_applied === false` 면 면제.
  → 대시보드(`g_h_i_dashboard.html`)와 명세서(`contractor_roster.html`) **양쪽 모두** 같은 규칙을 써야 합니다.

  ⚠️ **위 규칙은 '기본값'이지 전부가 아닙니다. 가구마다 다릅니다.** (2026-08-05 확인)
  - **월세에 관리비가 포함된** 가구가 있습니다
  - **월세에 부가세가 포함된** 가구가 있습니다
  - **세금 환급을 받는 가구만 부가세를 따로** 받습니다

  그래서 **월세납부 화면(`monthly_rent_collection.html`) 좌측 폼의 계산을 고치지 마세요.**
  거기 `부가세 적용` · `월세` · `관리비` 체크박스와 금액칸이 있어서 **직원이 그 가구에 맞게
  직접 정합니다.** 자동으로 맞히려 들면 오히려 틀립니다.
  우측 목록의 `(월세+관리비)×10%` 는 수납 전 **어림값**일 뿐 실제 청구가 아닙니다.
  화면 하나만 보고 "이중청구 버그"로 판단하지 마세요 — 한 번 그렇게 오판한 적이 있습니다.
- **관리비는 세전 금액으로 저장합니다.** 부가세는 화면에서 따로 계산해 보여 줍니다.
  (2026-07-31: 부가세 기능이 없던 시절 직원이 부가세를 합쳐 넣어 둔 4건 —
  443·101·102·201호 — 을 세전으로 되돌렸습니다. 총액은 그대로입니다.)
- **수도 요율(서울시 일반용)**: 상수도 1,270 + 하수도 1,550 + 물이용부담금 170 = **2,990원/톤**
  실제 고지액보다 비싸게 나오는 차액은 **건물주가 부담**합니다.
  (8·9층 펌프가 6·7·8층 공용 수압의 물을 끌어가 톤 차이가 발생. 32mm 바이패스관 요금도 건물 부담)
- **8·9층(아파트 20세대)**: 계량기 값이 우리에게 오지 않음 → **금액만** 입력
- **1000호**: 건물주 부담 (세입자 청구에서 제외)
- **한전 단독계약**: **상가층(3층 이하)에만** 존재. 검침 불가지만 저장·엑셀에서 빼지 말고 **0으로** 넣을 것
  → **103호가 한전 단독계약입니다.** 이 표시는 `rooms.meter_direct` 에 저장합니다.
  (2026-08-01 이전에는 브라우저 localStorage에만 있어서 **다른 PC에서 열면 사라졌습니다**)
- **1층은 3개 호실**. 지하2·지하1·2·3층은 층당 1행

### 실제 호실 구성 (2026-08-01 현장 확인)

- **400호는 철거되었습니다.** 예전 검침표에는 남아 있지만 지금은 없습니다
- **202·203호는 201호(병원)가 2층 전체를 씁니다** → 상태 `통합`
- **302·303호는 301호(노래방)가 3층 전체를 씁니다** → 상태 `통합`
- `통합` 상태인 방은 **공실로 세지 않고 검침표에도 줄을 만들지 않습니다.**
  나중에 나눠서 임대하면 상태만 `공실`로 되돌리면 됩니다. 호실 자료는 지우지 않았습니다
- 검침표는 상가를 호실번호가 아니라 **상호**로 적습니다
  (101=할리스커피, 102=명촌여행사, 201=병원2F, 301=노래방3F, B101=B1, B201=B2)

### 검침일은 항목마다 다릅니다 ⭐

| 항목 | 날짜 | 누가 하나 |
|---|---|---|
| 전기 | **11일** | **우리가 검침** → 한전에 메일 → 요금이 적혀서 회신됩니다 |
| 수도 | **9일** | **우리가 검침** → 요금표(톤당 2,990원)로 계산해 호실별 배분 |
| 가스 | **7일** | **우리는 검침 안 함.** 도시가스가 요금표를 건물로 보내오고 우리가 합산합니다 |

`bills.read_date_elec` / `read_date_water` / `read_date_gas` 에 각각 저장합니다.

**2026-08-01: 26년 6·7월 검침표(구글드라이브 PDF)를 읽어 132건을 넣었습니다.**
440~999호 60실의 전기·수도·가스 지침 + 800·1000호 전기 + 상가 수도 4곳입니다.
이제 8월 검침 년월을 고르면 7월 지침이 '지난 검침'에 채워집니다.

⚠️ **아래 세 곳은 검침표 종이에 그대로 그렇게 적혀 있습니다. 고치지 마세요.**
현장에서 계량기를 보고 직접 고칩니다.

| 호실 | 6월 → 7월 | |
|---|---|---|
| 449호 전기 | 12089 → 23347 | 한 달 11,258 사용 |
| 770호 전기 | 13774 → 13598 | 지침이 줄어듦 |
| 773호 전기 | 12831 → 12596 | 지침이 줄어듦 |

대조용 표: `claude_부동산/검침표_6월7월_대조.csv`

### 상가 수도 — 남·여는 화장실입니다 ⭐

검침표의 `수도 남 / 여` 는 **남자화장실·여자화장실 계량기**입니다.
공용 화장실이지만 **한 세입자가 그 층을 통째로 쓰면 점유 상태로 보고 그쪽에 청구**합니다.
→ 프로그램에는 **남+여를 더한 값 하나**로 넣습니다.

| 검침표 | 넣는 곳 | 6월 → 7월 | |
|---|---|---|---|
| 할리스커피 | **101호** | 10460 → 10495 | **물 쓰는 업종이라 전용 계량기** (화장실 아님) |
| 병원(2F) 남+여 | **201호** | 4142 → 4158 | 2529+1613 → 2532+1626 |
| 노래방(3F) 남+여 | **301호** | 3857 → 3857 | 881+2976 (두 달 같음) |
| (B1) | **B101호** | 2367 → 2367 | 여 칸만 있음 |
| 명촌여행사 | — | 계량기 없음 | |

### 상가 전기는 우리가 검침하지 않습니다 ⭐

검침표에서 **명촌여행사·할리스커피·병원2F·노래방3F·B1·B2 의 전기 칸은 전부 사선(X)** 입니다.
전부 한전 단독계약이라 `rooms.meter_direct = 1` 로 표시해 두었습니다
(101·102·103·201·301·B101·B201).
우리가 검침하는 상가 전기는 **400·800·1000호뿐**이고 400호는 철거됐습니다.
900호는 KT이며 검침표에 값이 없습니다.

**이걸 나눈 진짜 이유는 '전달 지침'입니다.** 검침하는 사람이 지난달 지침을 보고
사용량을 바로 확인해야 일이 됩니다. 예전에는 **저장한 날(`created_at`)** 로 몇 월분인지
판단해서, 8월에 7월분을 뒤늦게 넣으면 8월 검침으로 잡혀 **전달 지침이 엉뚱하게 떴습니다.**
지금은 **실제 검침일**과 화면에서 고른 **검침 년월**을 기준으로 봅니다.
년월을 바꾸면 그 달 기준으로 지난 검침·이번 검침이 다시 채워집니다.
- **계약 규칙**: 종료일 > 시작일, 최소 1개월 (달방 여행자 있음)

### 깔세와 '자동 연장' ⭐

**깔세는 매달 내면서 나갈 때까지 계속 사는 계약입니다.** 달마다 끝나는 게 아닙니다.

⚠️ **서류상 기간과 실제가 다릅니다. 이건 잘못 들어간 게 아닙니다.**
**정부가 달 단위 계약을 인정하지 않기 때문에**(전입신고·확정일자·임대차신고)
계약서에는 1년·2년으로 적고, 실제로는 달마다 받습니다.
그래서 661호가 13개월, 668호가 24개월로 들어가 있는 것은 **정상입니다. 고치지 마세요.**

프로그램에서는 `contracts.auto_extend = 1` 로 표시합니다.
계약 화면의 **[자동 연장]** 체크박스입니다. 나가시면 체크를 풀고 실제 퇴실일을 종료일에 적습니다.

**이번 기간 종료일 = 시작일 + n개월 − 1일. 자동 연장은 한 달씩 밉니다.**
1월 15일 시작이면 2월 14일이 딱 한 달입니다. 종료일에 달을 더하면
월말 계약(1월 31일 시작 등)이 하루씩 밀리므로 **반드시 시작일 기준으로** 계산합니다.

서류상 기간(1년·2년)만큼 밀지 않고 **한 달씩** 미는 이유:
깔세 세입자는 보증금을 적게 내는 대신 월세를 더 내고 **원할 때 나갈 자유**를 삽니다.
그래서 매달 월세를 받을 때가 곧 "연장하시겠어요?" 하고 묻는 시점입니다.
이번 기간 종료일이 **다음 월세 납입일 바로 전날**이 되어 사무실 흐름과 맞습니다.

이 규칙은 **`guard.js` 한 곳에만** 있습니다 (`contractEnd` · `contractAlive` · `contractExtended`).
화면마다 복사하지 마세요. 두 벌이 되면 어긋납니다.

2026-08-01: 깔세 11건(444·449·552·555·660·661·666·668·778·779·882)에 자동 연장을 켰습니다.
→ 공실 38실이 **27실**로, 만료 11건이 **0건**으로 바로잡혔습니다.

### 수납 약정일 — 계약 시작일과 같은 날 ⭐

**매월 며칠에 월세를 받는가.** 예전에는 모든 화면에 `매월 15일`이 **글자로 박혀** 있었고
계약서에 고칠 칸조차 없었습니다. 실제로는 계약마다 다릅니다. (2026-08-05 수정)

기준은 **계약 시작일과 같은 날**입니다. 1월 15일 시작이면 매월 15일.
이래야 '이번 기간 종료일(2월 14일)'이 다음 납입일 바로 전날이 되어 위 깔세 계산과 맞습니다.

계약서에 다른 날로 적혀 있을 때만 계약 화면 **[수납 약정일]** 칸에 넣습니다(`contracts.pay_day`).
비워 두면 0으로 저장되고 시작일 기준으로 봅니다 — 기존 계약 43건은 손댈 필요가 없습니다.

이 규칙도 **`guard.js` 한 곳에만** 있습니다 (`contractPayDay` · `contractPayDayText`).
- **호실·계약 중복 생성 금지** → 409 반환, "불러와 수정" 흐름
- 계약서가 없는 호실은 **공실**
- **세입자가 방을 옮기는 일이 잦습니다.** 이때 계약서는 그대로 따라갑니다.
  그래서 668호 일정에 `662.pdf` 가, 778호 일정에 `661...jpg` 가 붙어 있는 것은 **정상**입니다.
  파일명과 호실이 다르다고 해서 잘못 붙은 것으로 판단하지 마세요.
  캘린더에 `(777호가 옮김)`, `664서 방전환`, `(옛992호)` 처럼 이유가 적혀 있습니다.

## 3-2. 구글 캘린더가 현장의 기준 장부입니다

`대림빌딩업무` 캘린더(`jk2mii84c1flo91ru0svoha37k@group.calendar.google.com`)를
직원이 실제 업무 기준으로 씁니다. **여기가 사실상 원장입니다.**

- 일정 하나 = 계약 하나. 매달 반복(월세 납입일)
- 제목 형식: `호실/깔/계약일` 또는 `월세납입 443호하얀나비 /25.1.1`
  - `깔` = 깔세(선불 단기)
  - 뒤의 날짜가 **계약일**
- **계약서 PDF가 첨부**되어 있습니다 → 이것이 그 호실의 진짜 계약서입니다
- OCR 결과와 어긋나면 **캘린더를 믿으세요.** OCR은 지난 계약서를 읽는 일이 잦습니다

⚠️ **캘린더는 읽기만 합니다. 쓰지 마세요.** (2026-08-05)
직원들이 실제로 보고 일하는 곳이라 프로그램이나 작업자가 임의로 글을 남기면 안 됩니다.
"프로그램에 넣었음" 같은 표시도 붙이지 않습니다. 자료를 옮겨 왔으면 **프로그램 쪽 메모**에
출처를 적어 두면 됩니다 (예: 수납 memo에 "구글 캘린더 「오늘의 납부자 확인」 2026-08-03 기록").

**「오늘의 납부자 확인」 일정 읽는 법**: 설명란이 `1 994/1일/721,470` 형식입니다.
→ `994`=호실, `1일`=**수납 약정일**(계약 시작일과 같은 날), `721,470`=받은 금액.
맨 앞 숫자는 의미 없이 쓴 것이거나 깔세 표시이므로 **기록하지 않습니다.**

2026-07-31 기준 캘린더 계약 **48건**, 프로그램 DB **31건**.
아직 프로그램에 안 들어간 17개: 441·444·445·449·551·552·555·660·661·665·666·668·775·778·779·882·990
(대부분 깔세 단기) — 그래서 대시보드 공실 숫자가 실제보다 많이 나옵니다.

**301호(노래방 3F)도 계약이 안 들어가 있어 '공실'로 뜹니다.** 7월 검침표에는 영업 중입니다.
계약 내용은 현장이 정확하므로 **현장에서 직접 넣습니다.** 대신 만들지 마세요.

## 3-1. ⚠️ 오렌지파이 배포는 rsync 로 — git pull 을 쓰지 말 것

**`building_manager.db` 가 git에 추적되고 있습니다.**
그래서 오렌지파이에서 `git pull` 을 하면 **커밋에 들어있는 DB가 현장 실데이터를 조용히 덮어씁니다.**

- 그날 입력한 계약·수납·검침이 **사라집니다**
- 오류 메시지가 없습니다. 화면은 멀쩡해 보입니다
- 며칠 지나서야 "어? 그때 넣은 게 없네" 하고 알게 됩니다 → 그땐 복구가 어렵습니다

**→ 해결: 배포는 git이 아니라 rsync 로 합니다. DB가 이동 경로에 아예 없습니다.**

맥에서 실행 (한 덩어리로 복사해 붙여넣기):

```bash
rsync -a --delete-after --exclude '.git' --exclude '_backups' --exclude 'uploads' \
  --exclude 'building_manager*.db' --exclude '__pycache__' --exclude '.claude' \
  --exclude 'drive_backup_path.txt' \
  ~/rental-v2-online/ uglywolf@100.114.91.81:rental-v2-online/ && \
ssh root@100.114.91.81 'systemctl restart rental'
```

제외 항목을 **하나도 빼지 마세요.** 각각 이유가 있습니다.

| 제외 | 이유 |
|---|---|
| `building_manager*.db` | **현장 실데이터.** 이게 핵심입니다 |
| `uploads` | 계약서·신분증 스캔 원본 |
| `_backups` | 기기별 자동 백업 |
| `drive_backup_path.txt` | 백업 경로 설정 — **기기마다 달라야 함** |
| `.git` `__pycache__` `.claude` | 옮길 이유 없음 |

rsync는 제외한 파일을 **삭제 대상에서도 빼줍니다.** `--delete-after` 를 써도 오렌지파이의
DB·uploads·백업은 안전합니다. 전송이 끝난 뒤에 지우므로 중간에 끊겨도 문제 없습니다.

**DB를 옮겨야 할 때만** 따로, 의도적으로 `scp` 를 씁니다. 그때도 받는 쪽 DB를 먼저 복사해 두세요.

## 4. 지금까지 고친 대표적인 함정

- `if (!fr.src)` 로 iframe 로딩 판정하면 항상 참 → `dataset.loaded` 사용
- 직원 로그인 쿼리에 `OR id=999` 넣으면 마스터가 항상 걸림
- 가스·통신 저장 시 전기·수도 값이 0으로 덮이는 사고 → 기존 값 보존 필수
- 명세서에서 `find()` 로 고지를 고르면 옛날 달이 걸림 → **id가 가장 큰 것**을 명시적으로 선택
  - **금일현황도 같은 병이었다** (2026-08-05). 미납 목록에 그 호실의 고지를 **전부** 올려서
    미납이 132건으로 뜨고(실제 66곳), 값을 고치거나 [되돌리기]로 되돌려도 **옛 줄이 남아
    이전 값이 계속 보였다.** 임차인·연락처도 '먼저 나온 계약'을 써서 나간 세입자가 잡힐 수 있었다.
    → 호실별(공용부는 건물+구역별) **최신 1건**만 본다. `/api/v1/contracts` 에는 ORDER BY가
    없으므로 순서를 믿지 말고 화면에서 id로 명시적으로 고를 것.
- **자바스크립트는 숫자 0을 거짓으로 본다.** `x ? x : ''` 로 기존값을 채우면 **0원이 빈칸이 된다.**
  공과금 화면이 이랬다(2026-08-05): 0원을 저장해도 다시 열면 빈칸 → 네 칸이 다 비면
  "이 줄은 비어 있습니다"로 막혀 **0원으로는 고칠 수가 없었다.**
  0원은 실제로 있는 값이다(공실·한전 단독계약). 빈칸과 0을 반드시 구분할 것.
- PyInstaller는 **Python 3.13**으로 (3.14는 DLL 문제)
- 샌드박스에서 마운트 폴더의 SQLite는 "disk I/O error" → /tmp 로 복사 후 작업
- **없는 파일에 로그인 화면을 대신 내보내면 원인이 숨는다** (2026-07-31, 반나절 허비)
  계약서 PDF가 서버에 없었는데 `server.py` 가 `index.html` 을 200으로 내보내
  PDF 틀 안에 로그인 화면이 떴다 → "비밀번호를 묻는다"로 보였다.
  → 지금은 pdf·png·js 등 첨부파일류는 **정직하게 404**. 화면(html)만 로그인으로 보낸다.
  교훈: 증상이 "로그인"이어도 **네트워크 탭에서 실제 요청·응답부터 볼 것.**
- **화면·기능 파일(html·js·css)은 캐시 금지 헤더 필수.** 안 붙이면 업데이트해도
  브라우저가 옛 파일을 계속 써서 "고쳤는데 안 고쳐진" 상황이 된다.
- 대시보드 상단 카드는 **한때 숫자가 코드에 박혀 있었다**(4개동 180호실 등).
  화면에 보이는 수치는 반드시 실제 자료로 계산할 것.
- `openDocs` 를 화면 파일에서 다시 정의하면 `guard.js` 것을 덮어써 로그인 정보가 안 넘어간다.
- **API 시험을 실제 DB에 대고 하지 말 것** (2026-08-05)
  새 칸이 저장되는지 보려고 `POST /api/v1/contracts` 를 진짜 DB에 쐈다가
  계약 7번(554호)의 주소·관리비·계약서 첨부·특약이 통째로 날아갔다.
  POST 본문에 안 넣은 항목은 **빈 값으로 덮인다.** 되돌리긴 했지만 실 데이터였으면 큰일이다.
  → 시험은 **DB 사본**에 대고 하고, 끝나면 전 테이블을 원본과 대조해 차이 0건을 확인할 것.
- **한 칸을 두 화면이 다른 뜻으로 쓰면 언젠가 덮인다** (2026-08-05)
  계약서의 **특약**과 금일현황의 **수납 통화 메모**가 `contracts.special_terms` 한 칸을
  같이 쓰고 있었다. 금일현황에서 "오후 3시 입금 확약" 한 줄을 적으면 계약서 특약이
  **통째로 사라진다.** 특약이 실제로 들어간 계약이 없어서 여태 안 터졌을 뿐이다.
  부동산 계약은 특약이 없는 경우가 거의 없다.
  → `collect_memo` 칸을 새로 만들어 완전히 갈랐다.
    `special_terms` = 계약서 특약 (계약서 화면에서만 수정)
    `collect_memo`  = 그날의 수납 통화 메모 (금일현황에서만 수정)
  옛 화면이 캐시에 남아 `{id, special_terms}` 만 보내는 경우의 **안전망 갈래는 남겨 둘 것.**
  지우면 전체 UPDATE로 떨어져 계약서가 통째로 비워진다.
- **숫자만 보고 계약 조건을 추측하지 말 것 — DB에 없는 조건이 있다** (2026-08-05)
  885호 특약에 "공과금 15만원 초과분을 **임대료66만원**에 합산" 이라 적혀 있는데
  이 계약의 차임은 75만원이다. 사용자 확인 결과 —
  - **885호는 공과금을 건물이 부담하는 조건**으로 들어온 세입자다. 그래서 차임이
    깔세 표준가 66만원보다 **9만원 높은 75만원**이다. (9만원이 공과금 부담분)
  - 특약의 "66만원"은 **다른 깔세 계약서 양식에서 딸려온 숫자**다. 66만원이 건물
    표준가라 다른 계약서에 그렇게 적혀 있었고, 그 문구가 그대로 따라왔다.
  - 특약 3의 실제 뜻은 **"공과금이 15만원을 넘으면 75만원 + 초과분"** 이다.
  → 나는 처음에 이걸 오타로 봤다가, 깔세 10건 중 6건이 66만원인 걸 보고
    "표준가니까 의도적 기준선"이라고 **정반대로 단정했다.** 둘 다 틀렸다.
    통계는 맞았지만(66만=표준가) 거기서 끌어낸 추론이 틀렸다.
  → **공과금을 누가 내는지는 DB 어디에도 없다.** 금액이 안 맞아 보이면 추측하지 말고
    사용자에게 물을 것. 계약 조건의 상당수가 시스템에 기록돼 있지 않다.
  → 계약서 원문은 임차인 서명이 들어간 문서다. **사용자 지시 없이 고치지 않는다.**
    (2026-08-05 현재: 현장에서 확인 후 일괄 수정할지 결정하기로 함 — 그때까지 원문 유지)

  ⚠️ **885호 공과금 청구 주의** — 건물 부담 조건인데 시스템은 이걸 모른다.
  공과금 고지를 낼 때 885호에 그대로 청구되지 않는지 확인할 것. (15만원 초과분만 청구)

## 5. 계정

- 마스터: `uglywolf@gmail.com` (비밀번호는 sha256 해시로만 저장, 평문 없음)
- 직원 계정 생성은 **사용자 본인이 합니다.** 대신 만들지 마세요.

## 6. 다음 할 일 — 오렌지파이 서버 이전 ⭐

**→ 자세한 내용은 `ORANGEPI_SETUP.md` 를 읽으세요.**

한 줄 요약: 현장까지 30~40km를 오가는 게 비효율적이라, Orange Pi Zero 2W(4GB)를
건물에 두고 24시간 켜 두어 집에서 관리하려 합니다. 하드웨어는 이미 손에 있습니다.

## 7. 이 폴더 밖에 있는 자료

내용을 여기에 옮겨 적지 마세요. **가리키기만 합니다.** 두 벌이 되면 어긋납니다.

| 무엇 | 어디 |
|---|---|
| 계약서 OCR 결과 (md 135건) | `~/대림빌딩_계약서_md/` — 먼저 그 폴더의 `README.md` |
| 계약서 원본 스캔 | 구글드라이브 `~/Google Drive/내 드라이브/대림빌딩업무/손실장/{장기계약서,단기계약서}` |
| **월별 검침표 원본 (24~26년)** | 같은 곳 `손실장/공과금검침/{24년도,25년도,26년도}/*.pdf` — **현장이 실제로 쓰는 양식** |
| 오렌지파이 구축·이관 절차 | `ORANGEPI_SETUP.md` (이 폴더) |

계약서 OCR은 2026-08-01에 144건을 처리했습니다. 계약서 114건, 호실 50개 확인,
사람이 대조해야 할 34건은 `_점검필요.md` 에 있습니다.
**이 결과는 읽기 전용 중간 산출물이고 `building_manager.db` 에는 아무것도 넣지 않았습니다.**

다시 돌릴 일이 생기면 `~/대림빌딩_계약서_md/_스크립트/` 에 스크립트 3개가 있습니다.
PaddleOCR은 `~/.hermes/hermes-agent/venv/bin/python` 에 설치돼 있습니다.

⚠️ 맥은 한글 파일명을 **자모 분리(NFD)** 로 저장합니다. `find -name "*계약*"` 처럼
NFC로 친 패턴은 하나도 안 걸립니다. 한글 파일명을 다룰 때 반드시 정규화하세요.
