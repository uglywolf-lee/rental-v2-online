# 계약서 OCR 자동채움 기능 안내

계약서(B) 화면에서 **PDF/사진을 업로드하면 OCR로 칸을 자동으로 채우고, 사람이 확인·수정 후 저장**하는 기능입니다.

## 구성 요소
- `contract_master.html` — 계약서 등록 화면 (업로드 → OCR 채우기 → 편집 → 저장, 우측 원본 뷰어 대조)
- `ocr_engine.py` — 오프라인 OCR 모듈 (Tesseract 기반, 표준 임대차계약서/신분증/여권 필드 추출)
- `POST /api/v1/ocr` — `{filepath, doc_type}` → 추정 필드 + 신뢰도 + 원문 반환
- `install_ocr.sh` — 구동 PC에 OCR 엔진 설치

## 설치 (구동 PC에서 최초 1회)
```
bash install_ocr.sh
```
설치 항목: `tesseract` + 한글데이터 `kor` + `poppler`(PDF 변환). 설치 후 서버 재시작.
> 엔진이 없어도 서버는 정상 동작하며, OCR 시 "엔진 미설치" 안내만 표시됩니다. (무설치 포터블 성격은 OCR 기능에 한해서만 깨집니다.)

## 사용 순서
1. 계약서(B) 화면 → 문서 업로드(PDF/사진), 문서종류 선택(계약서/신분증/여권)
2. **[OCR 채우기]** 클릭 → 노란색 칸에 추정값이 채워짐 (신뢰도 배지: 높음/보통/낮음)
3. **우측 원본과 대조**하며 값 확인·수정
4. 저장 → 계약 정보와 함께 원본 문서 경로가 첨부됨

## 정확도 · 주의
- **여권**: 하단 MRZ 파싱으로 영문 성/이름·여권번호·국적·생년월일이 안정적으로 추출됩니다.
- **표준 임대차계약서**: 라벨 고정형이라 금액·날짜·주민번호·전화 등 매핑이 잘 됩니다.
- **자유서식/필기·저해상 스캔**: 정확도가 떨어집니다. 특히 주소·숫자에서 오탈자가 나므로 **반드시 검토**가 필요합니다.
- OCR 값은 모두 "추정치"이며, 최종 책임은 사람의 확인에 있습니다.

## 개인정보 보호
- 주민등록번호는 저장 시 뒷자리를 자동 마스킹합니다 (예: `480910-1******`).
- 오프라인 엔진이라 이미지가 외부로 전송되지 않습니다. 원본 파일은 `uploads/documents/` 에 보관됩니다.
